# FastAPI background tasks and async tests

## Project setup

```bash
python -m venv env
source env/bin/activate
pip install -r requirements.txt
```

## Running the project

```bash
uvicorn main:app --reload
```
